window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["FR-WEB-002"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<ol><li>The application MUST integrate with the PubChem PUG REST API to fetch comprehensive periodic table data in CSV format.</li><li>The periodic table payload MUST be parsed into structured TypeScript objects containing identifiers, physical, chemical, and categorical data.</li><li>The specific \"Period\" property MUST be appended programmatically based on atomic numbers to correctly map the 118 elements geometrically, as the raw PubChem data omits this.</li><li>The application MUST support querying chemical compounds by name to retrieve Structure-Data File (SDF) formatting containing Cartesian coordinates for 3D molecular rendering.</li><li>A client-side caching mechanism SHOULD be implemented to reduce network overhead for identical compound fetches.</li></ol>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why use PubChem PUG REST? (§1 #1)</strong> Hardcoding 118 elements' data and infinite molecules is prone to obsolescence and bloat. PubChem provides authoritative, real-time data without SOAP envelope overhead. <strong>Why append the Period manually? (§1 #3)</strong> The raw dataset from PubChem contains 17 columns but explicitly lacks the \"Period\" value required to draw the standard geometric grid. <strong>Why SDF coordinates? (§1 #4)</strong> SDF provides strict Cartesian spatial coordinates alongside complex covalent bonding information, which is critical for accurate WebGL representation.</p>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-typescript\">// src/services/pubchem.ts\nexport interface ElementData {\n  AtomicNumber: number;\n  Symbol: string;\n  Name: string;\n  AtomicMass: number;\n  AtomicRadius?: number;\n  Electronegativity?: number;\n  Period: number;\n}\n\nexport const fetchPeriodicTable = async (): Promise&lt;ElementData[]&gt; =&gt; {\n  // fetch from https://pubchem.ncbi.nlm.nih.gov/rest/pug/periodictable/JSON\n};\n\nexport const fetchCompoundSDF = async (name: string): Promise&lt;string&gt; =&gt; {\n  // fetch from https://pubchem.ncbi.nlm.nih.gov/rest/pug/compound/name/{name}/SDF\n};</code></pre>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Periodic Table Fetch</strong> — The <code>fetchPeriodicTable</code> function MUST successfully download and parse the 118 elements.</li><li><strong>Period Property Appended</strong> — Every returned <code>ElementData</code> object MUST contain a valid <code>Period</code> number (1-7) accurately derived from its <code>AtomicNumber</code>.</li><li><strong>SDF Fetch</strong> — The <code>fetchCompoundSDF</code> function MUST return the raw SDF text payload for a valid compound name like \"glucose\".</li></ol>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-typescript\">import { fetchPeriodicTable, fetchCompoundSDF } from './pubchem';\n\ntest('fetches elements and appends period', async () =&gt; {\n  const data = await fetchPeriodicTable();\n  expect(data.length).toBeGreaterThanOrEqual(118);\n  const hydrogen = data.find(e =&gt; e.Symbol === 'H');\n  expect(hydrogen?.Period).toBe(1);\n});\n\ntest('fetches SDF', async () =&gt; {\n  const sdf = await fetchCompoundSDF('water');\n  expect(sdf).toContain('V2000'); // Standard SDF format marker\n});</code></pre>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API contract above is the skeleton.)</p>\n<h2>§7 — Dependencies</h2>\n<ul><li>Upstream: FR-WEB-001</li><li>Downstream: FR-WEB-003</li></ul>\n<h2>§8 — Example payloads</h2>\n<p>N/A</p>\n<h2>§9 — Open questions</h2>\n<p>All resolved.</p>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>PubChem API down</td><td>HTTP 5xx</td><td>App shows error</td><td>Retry with backoff / Use static fallback</td></tr><tr><td>CORS policy blocks fetch</td><td>Browser console</td><td>Network Error</td><td>Proxy request (PubChem usually supports CORS)</td></tr><tr><td>Compound not found</td><td>HTTP 404</td><td>Error modal</td><td>Display \"Compound not found\" to user</td></tr><tr><td>Invalid JSON/CSV structure</td><td>Parsing error</td><td>Broken state</td><td>Validate schema strictly</td></tr><tr><td>Network timeout</td><td>Promise rejection</td><td>UI hangs</td><td>Implement abort controller and timeout</td></tr><tr><td>Caching memory bloat</td><td>High RAM usage</td><td>Slow browser</td><td>LRU cache with eviction policy</td></tr><tr><td>Period logic breaks</td><td>UI grid broken</td><td>Table skewed</td><td>Add strict tests for Period calculation</td></tr><tr><td>Rate limiting (429)</td><td>HTTP 429</td><td>Fetch blocked</td><td>Implement rate limiter on client</td></tr><tr><td>Partial payload</td><td>Truncated SDF</td><td>Rendering fails</td><td>Validate SDF integrity</td></tr><tr><td>Name disambiguation</td><td>Multiple isomers</td><td>Wrong molecule</td><td>Prompt user to select isomer</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li>PubChem JSON payload for periodic table is nested heavily under <code>Table.Row</code>. A mapping layer will be required to flatten this into the <code>ElementData</code> interface.</li><li>SDF text must be preserved with exact line breaks, as parsers (like 3Dmol.js) rely on strict formatting.</li></ul>\n<p><em>End of FR-WEB-002.</em></p>";
