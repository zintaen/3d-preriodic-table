window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-WEB-003"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<ol><li>The application MUST integrate the <code>3dmol</code> library to abstract complex WebGL primitives for molecular visualization.</li><li>A React component (<code>MoleculeViewer</code>) MUST be created to mount the 3Dmol.js canvas and inject the SDF spatial coordinates fetched from PubChem.</li><li>The component MUST support dynamic restyling between standard educational modes: stick, sphere (ball-and-stick), and cartoon (volumetric).</li><li>The viewer MUST instantiate intuitive mouse/touch controls for rotation, translation, and fluid zooming as natively provided by the library.</li><li>The component MUST safely dispose of its WebGL context when unmounted to prevent memory leaks and maintain browser performance.</li></ol>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why use 3Dmol.js? (§1 #1)</strong> Attempting to build a bespoke rendering engine for VSEPR geometries from fundamental WebGL primitives is highly inefficient. 3Dmol.js is highly optimized and operates natively on WebGL. <strong>Why different styling modes? (§1 #3)</strong> Different concepts require different abstractions. Wireframes show structure, ball-and-stick shows atoms clearly, while volumetric surfaces are needed for complex macromolecules. <strong>Why strict cleanup? (§1 #5)</strong> WebGL contexts are heavy. If multiple molecules are viewed over a session, failing to unmount the viewer will quickly exhaust the browser's context limit and crash the application.</p>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-typescript\">// src/components/MoleculeViewer.tsx\nimport React, { useEffect, useRef } from 'react';\n\nexport interface MoleculeViewerProps {\n  sdfData: string;\n  styleMode?: 'stick' | 'sphere' | 'cartoon';\n}\n\nexport const MoleculeViewer: React.FC&lt;MoleculeViewerProps&gt; = ({ sdfData, styleMode = 'stick' }) =&gt; {\n  // Mount 3dmol GLViewer here\n  return &lt;div id=\"viewer-container\" style={{ width: '100%', height: '100%' }}&gt;&lt;/div&gt;;\n};</code></pre>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Mounting</strong> — The <code>MoleculeViewer</code> MUST render a canvas element containing a 3D rendering of the provided SDF data.</li><li><strong>Styling</strong> — Changing the <code>styleMode</code> prop MUST dynamically update the visualization style of the molecule without full remounting.</li><li><strong>Cleanup</strong> — Unmounting the component MUST completely destroy the associated 3Dmol GLViewer instance.</li></ol>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-typescript\">// testing via Cypress or heavy integration testing due to WebGL limitations in Jest\nimport { mount } from '@cypress/react';\nimport { MoleculeViewer } from './MoleculeViewer';\n\nit('renders canvas', () =&gt; {\n  mount(&lt;MoleculeViewer sdfData=\"fake-sdf-string\" /&gt;);\n  cy.get('canvas').should('exist');\n});</code></pre>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API contract above is the skeleton.)</p>\n<h2>§7 — Dependencies</h2>\n<ul><li>Upstream: TASK-WEB-002</li><li>Downstream: TASK-WEB-004, TASK-WEB-005</li></ul>\n<h2>§8 — Example payloads</h2>\n<p>N/A</p>\n<h2>§9 — Open questions</h2>\n<p>All resolved.</p>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Invalid SDF data</td><td>3Dmol throws error</td><td>Blank canvas</td><td>Catch error, display \"Invalid Molecule\"</td></tr><tr><td>WebGL context limit</td><td>Browser console error</td><td>Black screen</td><td>Enforce strict <code>removeAllModels()</code> on unmount</td></tr><tr><td>Window resize bug</td><td>Canvas distorted</td><td>Stretched image</td><td>Bind resize observer to viewer <code>resize()</code></td></tr><tr><td>React Strict Mode double-mount</td><td>Multiple viewers</td><td>Z-fighting</td><td>Clean up in <code>useEffect</code> return block</td></tr><tr><td>Unsupported style string</td><td>GLViewer fails to style</td><td>Default stick style</td><td>Strongly type the style prop</td></tr><tr><td>Memory leak</td><td>DevTools heap snapshot</td><td>Browser crash</td><td>Audit WebGL contexts and JS objects</td></tr><tr><td>Missing line breaks in SDF</td><td>Parser fails</td><td>Empty viewer</td><td>Sanitize SDF string before passing</td></tr><tr><td>Container has no height</td><td>CSS layout</td><td>Invisible canvas</td><td>Enforce minimum height/flex on wrapper</td></tr><tr><td>Touch events swallowed</td><td>Mobile UX broken</td><td>Can't scroll page</td><td>Configure touch event propagation</td></tr><tr><td>Library version conflict</td><td><code>npm install</code></td><td>Broken build</td><td>Pin <code>3dmol</code> version in package.json</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li>3Dmol.js requires a DOM element to attach to. <code>useRef</code> must be used to grab the div and instantiate <code>$3Dmol.createViewer()</code>.</li></ul>\n<p><em>End of TASK-WEB-003.</em></p>";
