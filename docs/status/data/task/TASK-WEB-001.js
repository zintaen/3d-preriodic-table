window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-WEB-001"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<ol><li>The project MUST be initialized using Vite with the React and TypeScript template.</li><li>Tailwind CSS MUST be configured with the CyberSkill DESIGN.md anchors (<code>#45210E</code> Umber, <code>#F4BA17</code> Ochre).</li><li>The typography MUST be configured to use <code>Be Vietnam Pro</code> for UI text and <code>JetBrains Mono</code> for code blocks.</li><li>The project MUST support Vietnamese as a first-class language, ensuring line heights are configured to prevent diacritic clipping (min 1.5 for body, 1.35 for headings).</li><li>Code style MUST be enforced using ESLint and Prettier.</li></ol>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why Vite + React? (§1 #1)</strong> React is required by the strategy document to support React Three Fiber for 3D visualization. Vite provides the fastest dev environment. <strong>Why Tailwind CSS with custom tokens? (§1 #2)</strong> To easily adapt the design system tokens from DESIGN.md without writing massive raw CSS files. <strong>Why Be Vietnam Pro and specific line heights? (§1 #3, #4)</strong> DESIGN.md mandates Vietnamese as a first-class language. The specified line heights are critical to prevent stacked diacritics from clipping.</p>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-typescript\">// tailwind.config.js snippet\nexport default {\n  theme: {\n    extend: {\n      colors: {\n        umber: '#45210E',\n        ochre: '#F4BA17',\n      },\n      fontFamily: {\n        sans: ['\"Be Vietnam Pro\"', 'sans-serif'],\n        mono: ['\"JetBrains Mono\"', 'monospace'],\n      },\n    },\n  },\n}</code></pre>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Vite Init</strong> — The application MUST start a dev server using <code>npm run dev</code> that renders a basic React page.</li><li><strong>Tailwind Config</strong> — Tailwind classes for <code>text-umber</code> and <code>bg-ochre</code> MUST be available and apply the correct hex codes.</li><li><strong>Typography Support</strong> — Text rendered in the app MUST use <code>Be Vietnam Pro</code> and the CSS must enforce line-heights of at least 1.5 for body text.</li></ol>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-typescript\">// App.test.tsx\nimport { render, screen } from '@testing-library/react';\nimport App from './App';\n\ntest('renders app and typography', () =&gt; {\n  const { container } = render(&lt;App /&gt;);\n  expect(container.firstChild).toBeInTheDocument();\n});</code></pre>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API contract above is the skeleton.)</p>\n<h2>§7 — Dependencies</h2>\n<ul><li>Upstream: None</li><li>Downstream: TASK-WEB-002, TASK-WEB-006</li></ul>\n<h2>§8 — Example payloads</h2>\n<p>N/A for scaffolding.</p>\n<h2>§9 — Open questions</h2>\n<p>All resolved.</p>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Vite fails to build</td><td>CI build step</td><td>Build error</td><td>Fix TS/Vite config</td></tr><tr><td>Tailwind classes not applied</td><td>Visual regression / CI</td><td>Unstyled UI</td><td>Verify postcss config</td></tr><tr><td>Fonts fail to load</td><td>Network tab</td><td>Fallback fonts</td><td>Check font URLs</td></tr><tr><td>Diacritic clipping</td><td>Manual visual test</td><td>Unreadable text</td><td>Fix line-height</td></tr><tr><td>ESLint fails</td><td>pre-commit hook</td><td>Commit blocked</td><td>Fix lint errors</td></tr><tr><td>Tests fail</td><td><code>npm test</code></td><td>CI fail</td><td>Fix test assertions</td></tr><tr><td>Port conflict</td><td><code>npm run dev</code></td><td>Server start fail</td><td>Use dynamic port</td></tr><tr><td>Node version mismatch</td><td><code>npm install</code></td><td>Install error</td><td>Use <code>.nvmrc</code></td></tr><tr><td>TS strict mode error</td><td><code>tsc --noEmit</code></td><td>Build blocked</td><td>Fix types</td></tr><tr><td>Missing peer dependencies</td><td><code>npm install</code></td><td>Warning/Error</td><td>Install peers</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li>Run <code>npx create-vite@latest . --template react-ts</code> to initialize.</li><li>Install tailwindcss, postcss, and autoprefixer as dev dependencies.</li><li>Add Google Fonts link in <code>index.html</code>.</li></ul>\n<p><em>End of TASK-WEB-001.</em></p>";
