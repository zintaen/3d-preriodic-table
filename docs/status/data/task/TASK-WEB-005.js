window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-WEB-005"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<ol><li>The application MUST implement an interactive \"Atom Builder\" sandbox where users can incrementally add protons, neutrons, and electrons.</li><li>The state of this sandbox MUST be managed globally (e.g., via Zustand) to track the current composed element, its net charge, and its isotope stability.</li><li>The UI MUST provide immediate feedback when a composed element matches a known real element, triggering the <code>OrbitalViewer</code> and <code>MoleculeViewer</code> with relevant data.</li><li>The system MUST validate physical laws (e.g., rejecting an element with 5 protons and 0 neutrons as wildly unstable) and display educational error messages.</li></ol>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why a sandbox? (§1 #1)</strong> As per the strategy, \"Constructivist mechanics\" require users to build knowledge by doing. Building atoms from subatomic particles teaches atomic theory inherently. <strong>Why global state? (§1 #2)</strong> The constructed atom dictates what the entire rest of the dashboard (the orbitals, the data panel, the molecules) displays. <strong>Why physical validation? (§1 #4)</strong> If users can build nonsense without feedback, the educational value is negative.</p>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-typescript\">// src/store/gameStore.ts\nimport { create } from 'zustand';\n\ninterface GameState {\n  protons: number;\n  neutrons: number;\n  electrons: number;\n  addProton: () =&gt; void;\n  addNeutron: () =&gt; void;\n  addElectron: () =&gt; void;\n  reset: () =&gt; void;\n  getNetCharge: () =&gt; number;\n  getAtomicMass: () =&gt; number;\n}\n\nexport const useGameStore = create&lt;GameState&gt;((set, get) =&gt; ({\n  protons: 1,\n  neutrons: 0,\n  electrons: 1,\n  addProton: () =&gt; set((state) =&gt; ({ protons: state.protons + 1 })),\n  addNeutron: () =&gt; set((state) =&gt; ({ neutrons: state.neutrons + 1 })),\n  addElectron: () =&gt; set((state) =&gt; ({ electrons: state.electrons + 1 })),\n  reset: () =&gt; set({ protons: 1, neutrons: 0, electrons: 1 }),\n  getNetCharge: () =&gt; get().protons - get().electrons,\n  getAtomicMass: () =&gt; get().protons + get().neutrons,\n}));</code></pre>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>State mutation</strong> — The Zustand store MUST correctly track subatomic particles and compute derived properties (charge, mass).</li><li><strong>UI Controls</strong> — The user MUST be able to click buttons to add subatomic particles, which instantly updates the global state.</li><li><strong>Feedback loop</strong> — Constructing Carbon-12 (6p, 6n, 6e) MUST yield a stable indicator.</li></ol>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-typescript\">// src/store/gameStore.test.ts\nimport { useGameStore } from './gameStore';\n\ntest('store tracks particles and charge', () =&gt; {\n  const store = useGameStore.getState();\n  store.addProton();\n  expect(useGameStore.getState().protons).toBe(2);\n  expect(useGameStore.getState().getNetCharge()).toBe(1); // 2p, 1e\n});</code></pre>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API contract above is the skeleton.)</p>\n<h2>§7 — Dependencies</h2>\n<ul><li>Upstream: TASK-WEB-003, TASK-WEB-004</li><li>Downstream: TASK-WEB-006</li></ul>\n<h2>§8 — Example payloads</h2>\n<p>N/A</p>\n<h2>§9 — Open questions</h2>\n<p>All resolved.</p>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Unstable isotope logic wrong</td><td>Unit testing</td><td>Misinformation</td><td>Hardcode standard stable isotopes</td></tr><tr><td>Store hydration fails</td><td>Next.js/SSR</td><td>State mismatch</td><td>Disable SSR for this store</td></tr><tr><td>Infinite render loop</td><td>React re-renders</td><td>Browser crash</td><td>Use shallow selectors in Zustand</td></tr><tr><td>Negative particle counts</td><td>UI spamming</td><td>Math breaks</td><td>Clamp counts to &gt;= 0</td></tr><tr><td>Exceeding element 118</td><td>UI</td><td>Breaks PubChem map</td><td>Restrict protons &lt;= 118</td></tr><tr><td>Missing element data</td><td>Derived lookup</td><td>UI crash</td><td>Provide unknown element fallback</td></tr><tr><td>Poor animation performance</td><td>Drag/drop</td><td>Jittery UI</td><td>Use CSS transforms / Framer Motion</td></tr><tr><td>Gamification too punitive</td><td>Playtesting</td><td>User quits</td><td>Keep error messages constructive</td></tr><tr><td>Missing atomic mass decimals</td><td>Data mismatch</td><td>Confusion</td><td>Explain isotopes vs average mass</td></tr><tr><td>Reset button broken</td><td>QA</td><td>Stuck state</td><td>Ensure strict reset function</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li>Start with simple buttons before building complex drag-and-drop visuals.</li></ul>\n<p><em>End of TASK-WEB-005.</em></p>";
