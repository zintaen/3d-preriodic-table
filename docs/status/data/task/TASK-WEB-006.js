window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-WEB-006"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<ol><li>The application MUST implement the \"Liquid Glass\" aesthetic utilizing CSS backdrop-filters, subtle borders, and soft shadows over a dynamic background.</li><li>The overarching layout MUST be a responsive dashboard utilizing CSS Grid to house the periodic table, 3D viewers, and the atom builder interface.</li><li>The UI components MUST strictly utilize the <code>Be Vietnam Pro</code> font and adhere to the contrast tokens (<code>Umber</code>, <code>Ochre</code>) established in <code>DESIGN.md</code>.</li><li>The application MUST support i18n natively, allowing users to toggle between English and Vietnamese, with Vietnamese layouts extensively tested for word length and diacritic vertical spacing.</li></ol>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why Liquid Glass? (§1 #1)</strong> The strategy document asks for a modern, futuristic aesthetic. Glassmorphism provides this while allowing background elements (like orbital clouds) to show through the UI. <strong>Why Vietnamese testing? (§1 #4)</strong> <code>DESIGN.md</code> explicitly mandates Vietnamese as a first-class language. Vietnamese words have different wrapping characteristics than English and require taller line-heights.</p>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-tsx\">// src/components/GlassCard.tsx\nimport React from 'react';\n\nexport const GlassCard: React.FC&lt;React.PropsWithChildren&lt;{ className?: string }&gt;&gt; = ({ children, className }) =&gt; {\n  return (\n    &lt;div className={`backdrop-blur-md bg-white/10 border border-white/20 shadow-lg rounded-xl ${className || ''}`}&gt;\n      {children}\n    &lt;/div&gt;\n  );\n};</code></pre>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>Aesthetics</strong> — The <code>GlassCard</code> MUST render with a backdrop blur and semi-transparent background.</li><li><strong>Layout</strong> — The <code>DashboardLayout</code> MUST arrange its children without overlapping on desktop displays.</li><li><strong>Typography</strong> — All text within the UI MUST visually match the <code>Be Vietnam Pro</code> font family.</li><li><strong>i18n</strong> — Toggling a language state MUST translate key UI strings (e.g., \"Protons\" -&gt; \"Proton\").</li></ol>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-typescript\">// src/components/GlassCard.test.tsx\nimport { render } from '@testing-library/react';\nimport { GlassCard } from './GlassCard';\n\ntest('applies glassmorphism classes', () =&gt; {\n  const { container } = render(&lt;GlassCard&gt;Content&lt;/GlassCard&gt;);\n  const div = container.firstChild as HTMLDivElement;\n  expect(div.className).toContain('backdrop-blur');\n});</code></pre>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API contract above is the skeleton.)</p>\n<h2>§7 — Dependencies</h2>\n<ul><li>Upstream: TASK-WEB-001, TASK-WEB-005</li><li>Downstream: None</li></ul>\n<h2>§8 — Example payloads</h2>\n<p>N/A</p>\n<h2>§9 — Open questions</h2>\n<p>All resolved.</p>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Backdrop-filter unsupported</td><td>Old browser</td><td>Fallback color</td><td>Provide a solid rgba() fallback</td></tr><tr><td>Poor contrast</td><td>Lighthouse a11y</td><td>Unreadable text</td><td>Check WCAG ratios against background</td></tr><tr><td>Grid breaks on mobile</td><td>Resize window</td><td>Horizontal scroll</td><td>Implement strict flex-col breakpoints</td></tr><tr><td>Diacritics clipped</td><td>Visual check</td><td>Typo</td><td>Enforce line-height: 1.5+</td></tr><tr><td>Missing translation key</td><td>i18n runtime</td><td>Raw key shown</td><td>Fallback to English string</td></tr><tr><td>CSS bundle bloat</td><td>Build output</td><td>Slow load</td><td>PurgeCSS / Tailwind JIT handles this</td></tr><tr><td>Z-index wars</td><td>UI layers overlap</td><td>Modals hidden</td><td>Establish strict z-index scale</td></tr><tr><td>Animations jitter</td><td>Profiler</td><td>Low FPS</td><td>Use <code>transform</code> not <code>top/left</code></td></tr><tr><td>Font fails to load</td><td>Network</td><td>System font used</td><td>Preload critical font faces</td></tr><tr><td>Light mode toggle</td><td>User pref</td><td>Unreadable</td><td>Force dark mode or adapt glass colors</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li>Tailwind CSS makes glassmorphism trivial (<code>backdrop-blur-md bg-white/10</code>).</li></ul>\n<p><em>End of TASK-WEB-006.</em></p>";
