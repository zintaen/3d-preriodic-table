window.CS_SPEC=window.CS_SPEC||{};window.CS_SPEC["TASK-WEB-004"]="<h2>§1 — Description (BCP-14 normative)</h2>\n<ol><li>The application MUST provide a secondary 3D viewer (<code>OrbitalViewer</code>) powered by <code>react-three-fiber</code> and <code>three</code> to render quantum mechanical probability clouds (atomic orbitals).</li><li>The viewer MUST accurately represent the geometries of $s$, $p$, and $d$ orbitals using spherical harmonic functions or pre-computed mesh geometries.</li><li>The orbitals MUST be rendered using custom volumetric or transparent shaders to simulate electron probability density rather than solid opaque plastic.</li><li>The component MUST accept standard quantum numbers ($n$, $l$, $m_l$) as props to dynamically select and render the correct orbital shape.</li></ol>\n<h2>§2 — Why this design (rationale for humans)</h2>\n<p><strong>Why React Three Fiber instead of 3Dmol.js for this? (§1 #1)</strong> 3Dmol.js is optimized for molecules (balls and sticks). It cannot natively render complex arbitrary volumes like $d_{z^2}$ orbitals. React Three Fiber provides raw access to Three.js primitives and custom GLSL shaders needed for probability clouds. <strong>Why volumetric shaders? (§1 #3)</strong> An orbital is not a solid object; it's a probability distribution. Rendering it as a solid mesh is educationally inaccurate. Transparency and fresnel effects are required.</p>\n<h2>§3 — API contract</h2>\n<pre><code class=\"language-typescript\">// src/components/OrbitalViewer.tsx\nimport { Canvas } from '@react-three/fiber';\nimport React from 'react';\n\nexport interface OrbitalViewerProps {\n  n: number;\n  l: number; // 0=s, 1=p, 2=d\n  ml: number;\n}\n\nexport const OrbitalViewer: React.FC&lt;OrbitalViewerProps&gt; = ({ n, l, ml }) =&gt; {\n  return (\n    &lt;Canvas&gt;\n      {/* Orbital meshes and lights */}\n    &lt;/Canvas&gt;\n  );\n};</code></pre>\n<h2>§4 — Acceptance criteria</h2>\n<ol><li><strong>R3F Canvas</strong> — The component MUST render a valid Three.js canvas using <code>@react-three/fiber</code>.</li><li><strong>Prop Dependency</strong> — Passing $l=0$ MUST render a spherical shape, while $l=1$ MUST render a dumbbell shape.</li><li><strong>Shader Execution</strong> — The material applied to the orbital MUST utilize transparency and additive blending to look \"cloud-like\".</li></ol>\n<h2>§5 — Verification</h2>\n<pre><code class=\"language-typescript\">// integration test\nimport { render } from '@testing-library/react';\nimport { OrbitalViewer } from './OrbitalViewer';\n\ntest('renders R3F canvas element', () =&gt; {\n  const { container } = render(&lt;OrbitalViewer n={1} l={0} ml={0} /&gt;);\n  expect(container.querySelector('canvas')).toBeInTheDocument();\n});</code></pre>\n<h2>§6 — Implementation skeleton</h2>\n<p>(API contract above is the skeleton.)</p>\n<h2>§7 — Dependencies</h2>\n<ul><li>Upstream: TASK-WEB-003</li><li>Downstream: TASK-WEB-005</li></ul>\n<h2>§8 — Example payloads</h2>\n<p>N/A</p>\n<h2>§9 — Open questions</h2>\n<p>All resolved.</p>\n<h2>§10 — Failure modes inventory</h2>\n<table><thead><tr><th>Failure</th><th>Detection</th><th>Outcome</th><th>Recovery</th></tr></thead><tbody><tr><td>Invalid quantum numbers</td><td>Prop validation</td><td>Fallback to 1s</td><td>Clamp inputs to valid ranges</td></tr><tr><td>Shader compilation fails</td><td>WebGL error</td><td>Magenta fallback</td><td>Provide standard mesh material</td></tr><tr><td>Z-fighting with axes</td><td>Visual glitch</td><td>Flickering</td><td>Apply polygon offset / tweak depth</td></tr><tr><td>Canvas scaling blur</td><td>High DPI screen</td><td>Blurry render</td><td>Set <code>pixelRatio</code> to window.devicePixelRatio</td></tr><tr><td>Excessive polycount</td><td>Low FPS</td><td>Stuttering</td><td>Optimize geometries (LOD)</td></tr><tr><td>Missing Three.js peer deps</td><td>npm install</td><td>Build error</td><td>Install explicitly</td></tr><tr><td>Touch rotation broken</td><td>Mobile usage</td><td>Stuck model</td><td>Add <code>OrbitControls</code></td></tr><tr><td>Overlapping orbitals opaque</td><td>Visual testing</td><td>Looks solid</td><td>Enable <code>depthWrite=false</code></td></tr><tr><td>Memory leak</td><td>Unmounting</td><td>Slow browser</td><td>R3F handles most, check textures</td></tr><tr><td>Conflicting contexts</td><td>With 3Dmol viewer</td><td>Crash</td><td>Separate DOM trees strictly</td></tr></tbody></table>\n<h2>§11 — Implementation notes</h2>\n<ul><li>To save time, predefined parametric geometries for $p$ and $d$ orbitals can be approximated if complex spherical harmonic math in GLSL takes too long to write.</li></ul>\n<p><em>End of TASK-WEB-004.</em></p>";
